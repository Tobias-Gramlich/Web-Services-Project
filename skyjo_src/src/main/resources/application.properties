# --- Datasource ---
quarkus.datasource.db-kind=mariadb
quarkus.datasource.jdbc.url=jdbc:mariadb://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:skyjo}
quarkus.datasource.username=${DB_USER:app}
quarkus.datasource.password=${DB_PASS:app_pw}
quarkus.jooq.dialect=MARIADB
quarkus.http.port=8080
quarkus.http.access-log.enabled=true
quarkus.http.access-log.log-to-file=false
quarkus.http.access-log.pattern=common


quarkus.log.console.enable=true
quarkus.log.category."io.quarkus.http.access-log".level=INFO
quarkus.log.category."io.quarkus.http.access-log".use-parent-handlers=true


# --- Hibernate: aus, damit Flyway das Schema steuert ---
quarkus.hibernate-orm.database.generation=none

# --- Flyway: an ---
quarkus.flyway.migrate-at-start=true
quarkus.flyway.locations=classpath:db/migration

# Wenn die DB schon existiert (ohne flyway_schema_history):
quarkus.flyway.baseline-on-migrate=true

quarkus.http.cors=true
quarkus.http.cors.origins=http://10.50.15.53:3005
quarkus.http.cors.credentials=false
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
